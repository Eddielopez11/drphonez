$(document).ready(function(){
// ********* Header *********  //
// Successful repairs counter animation
  function animateValue(id, end, duration) {
    var start = end - 2000 ;
    var range = end - start;
    var current = start;
    var increment = end > start? 10 : -1;
    var stepTime = Math.abs(Math.floor(duration / range));
    var obj = document.getElementById(id);
    var timer = setInterval(function() {
        current += increment;
        obj.innerHTML = current;
        if (current == end) {
            clearInterval(timer);
        }
    }, stepTime);
  }

  animateValue("phonesFixed", {{ settings.successful_repairs }}, .01);

// ********* Main Page *********  //
// if($('body').hasClass('template-index')){
//
//  // slideshow
//    var slideIndex = 1;
//    showSlides(slideIndex);
//
//    function currentSlide(n) {
//      clearTimeout(autoSlide);
//      showSlides(slideIndex = n);
//   };
//
//    document.getElementById("dot1").onclick = function selectSlide() {
//      currentSlide(1);
//    };
//
//    document.getElementById("dot2").onclick = function selectSlide() {
//      currentSlide(2);
//    };
//
//    document.getElementById("dot3").onclick = function selectSlide() {
//      currentSlide(3);
//    };
//
//    document.getElementById("dot4").onclick = function selectSlide() {
//      currentSlide(4);
//    };
//
//    document.getElementById("dot5").onclick = function selectSlide() {
//      currentSlide(5);
//    };
//
//    document.getElementById("left-arrow").onclick = function slideDown() {
//      slideIndex--;
//      currentSlide(slideIndex);
//    };
//
//    document.getElementById("right-arrow").onclick = function slideUp() {
//      slideIndex++;
//      currentSlide(slideIndex);
//    };
//
//    function autoSlides(e) {
//      slideIndex++;
//      showSlides(slideIndex);
//    };
//
//    var autoSlide;
//    function showSlides(n) {
//      var slides = document.getElementsByClassName("newsSlides");
//      var dots = document.getElementsByClassName("dot");
//      if (n > slides.length) {slideIndex = 1}
//      if (n < 1) {slideIndex = slides.length}
//      for (var i = 0; i < slides.length; i++) {
//          slides[i].style.display = "none";
//      }
//      for (var i = 0; i < dots.length; i++) {
//          dots[i].className = dots[i].className.replace(" dot--active", "");
//      }
//      slides[slideIndex-1].style.display = "block";
//      slides[slideIndex-1].style.opacity = 1;
//      dots[slideIndex-1].className += " dot--active";
//      autoSlide = setTimeout(function(){autoSlides()}, 6500);
//
//    };
//  };
//
  // ********* FAQ Page *********  //

  $(window).click(function() {
  //Hide the menus if visible
  });

  $(".childshow").click(function (e) {
    e.stopPropagation();
    jQuery(this).children('.childshow').toggle("medium");
  });

  // ********* Cart Page *********  //

  $(".closeModal").click(function (e) {
    e.stopPropagation();
    jQuery(this).parent().parent('#tempModal').toggle();
  });


});


// ********* Locations Page *********  //

/* if($('body').has('#our-locations')){

    if(navigator.geolocation) {


      navigator.geolocation.getCurrentPosition(function(position) {
          var lat = position.coords.latitude;
          var long = position.coords.longitude;
          var point = new google.maps.LatLng(lat, long);
          new google.maps.Geocoder().geocode(
              {'latLng': point},
              function (res, status) {
                  var zip = res[0].formatted_address.match(/,\s\w{2}\s(\d{5})/);
                  $("#location").val(zip);
                  console.log(zip);
              }
          );
      });
  }

};

*/
